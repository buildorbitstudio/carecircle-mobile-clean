import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function AppointmentsScreen() {
  return (
    <PlaceholderScreen
      description="Visits, transportation, and family reminders."
      icon="calendar"
      nextStep="Appointment details, assigned drivers, and reminder preferences will be implemented here."
      title="Appointments"
    />
  );
}
